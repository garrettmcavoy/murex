uniform sampler2D tDiffuse; // The rendered scene's texture

void main() {
    vec2 uv = gl_FragCoord.xy / resolution.xy; // Calculate UV coordinates
    vec4 originalColor = texture2D(tDiffuse, uv); // Fetch the color of the current pixel

    gl_FragColor = originalColor; // Set the color of the current pixel
}
